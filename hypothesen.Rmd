---
title: "Hypothesen"
author: "Katharina Wagner"
date: "22 Januar 2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


```{r datensatz laden}

library(tidyverse)
library(haven)
library(sjlabelled)
library(knitr)
library(psych)

datensatz <- read.csv("datensatz.csv", sep = ";")
```

##Hypothesenüberprüfung
```{r}
## H1) Die Emotionalisierung durch Überschriften (in Kombination mit Bildern) in Utopia-Artikeln bewirkt einen Nudging-Effekt auf Rezipienten

cor.test(datensatz$group.stimuli, datensatz$mindset1)
cor.test(datensatz$group.stimui, datensatz$mindset2)


```

##Problem: 
- datensatz „e“ existiert nicht; alle Items mit „e“ eingeben funktioniert auch nicht
DENKFEHLER?
- mind1 und mind2 als Variable gibt es nicht; das gleiche wie bei „e“

```{r}
## H2) Bei Rezipienten, die einen nachhaltigen Lebensstil pflegen, gibt es einen Nudging-Effekt.

cor.test(datensatz$behavior_items, datensatz$mindset1, datensatz$mindset2)

```

##Problem:
Funktioniert nicht (mindset1 /mindset2)

```{r}
## H3) Ein Nudging-Effekt tritt vermehrt bei tatsächlichen Utopia-Nutzern auf.

wilcox.test (datensatz$group.stimuli ~datensatz$use_utopia_1)

```


```{r}
## H4) Ein Nudging-Effekt tritt häufiger bei Frauen auf.

datensatz %>% filter(gender != 3)

t.test(datensatz$gender ~ datensatz$group.stimuli)

```
```{r}
## H5) Es besteht ein Zusammenhang zwischen dem nachhaltigen Verhalten eines Nutzers und der empfundenen Emotionalisierung.

cor.test(datensatz$behavior_items, key_stimuli/scores_E, result_E)

##Problem: 🡪 funktioniert alles nicht,welche Variable für��� welche Variable für die empfundene Emtionalisierung?